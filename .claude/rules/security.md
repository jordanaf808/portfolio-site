---
paths:
  - 'src/pages/api/**'
  - 'src/**/*.ts'
  - 'src/**/*.tsx'
---

# Security

## Contact Form

The contact form is the only user-facing attack surface. Treat it accordingly.

### Input Validation (server-side, always)

Validate in the API route before calling Resend — never trust the client-side form:

```ts
const MAX_NAME = 100
const MAX_EMAIL = 254 // RFC 5321 limit
const MAX_MESSAGE = 2000

// Required checks
if (!name || typeof name !== 'string' || name.trim().length === 0) {
	/* 400 */
}
if (!email || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
	/* 400 */
}
if (!message || message.trim().length < 10) {
	/* 400 */
}

// Length limits — prevent abuse
if (name.length > MAX_NAME || message.length > MAX_MESSAGE) {
	/* 400 */
}
```

- Strip leading/trailing whitespace from all string inputs before using them
- Return generic error messages to the client (`"Invalid request"`) — log details server-side
- Return `400` for validation failures, `500` (with no details) for Resend errors

### Rate Limiting

`/api/contact` has no automatic abuse protection — add it explicitly. Options on Cloudflare Workers:

- **Workers Rate Limiting binding** (simplest in-Worker option) — declare a `ratelimits` binding in `wrangler.jsonc` (`simple.limit` + `simple.period`, where period is `10` or `60` seconds), then gate the handler: `const { success } = await locals.runtime.env.LIMITER.limit({ key })` and return `429` when `!success`. Prefer a stable key (route/identifier) over a raw IP.
- **Cloudflare WAF rate-limiting rules** (zone-level) as a perimeter layer in front of the Worker.
- **Cloudflare KV** for a bespoke persistent counter (Workers are stateless per request).
- At minimum (already in place): server-side Zod validation + Turnstile verification, so bots get `422`/`403` and never reach Resend.

### Environment Secrets

- `RESEND_API_KEY` is declared in `astro.config.mjs` (`env.schema`, context `server`, access `secret`) and must only ever be read via `import { RESEND_API_KEY } from 'astro:env/server'` in server-side code. Do **not** use `import.meta.env.RESEND_API_KEY`: under the v6 Cloudflare adapter SSR runs in workerd, where the secret is a runtime binding — `import.meta.env` is a build-time replacement that bakes the value into the bundle and breaks key rotation.
- Never reference `RESEND_API_KEY` in any `.astro` template or `.tsx` file — it would be bundled into client JS. (`astro:env/server` is server-only by construction and will error if imported client-side.)
- Never log the key, even partially
- If the key is ever committed or logged, rotate it immediately in Resend dashboard + Cloudflare Workers

## Cloudflare Workers Headers

Set the non-CSP security headers in `public/_headers` (Cloudflare Workers static-asset header file). **Workers caveat:** `_headers` rules apply only to **static asset responses**, *not* to responses generated by the Worker. Every page on this site is prerendered static **except** `/api/contact` (SSR), so the `/*` rules below cover the entire browsable site — but any header needed on the `/api/contact` response must be set on its `new Response()` in code, not here.

```
/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=()
  X-XSS-Protection: 1; mode=block
```

Do **not** add a `Content-Security-Policy` line here — it is owned by Astro (see below). `X-Frame-Options: DENY` is the framing control, since a `<meta>` CSP cannot express `frame-ancestors`.

## Content Security Policy

CSP is **not** a static header. It is generated by Astro's built-in `security.csp` in `astro.config.mjs` and injected per-page as a `<meta http-equiv="content-security-policy">`. Astro emits SHA-256 hashes for every inline script/style it ships (island hydration + component `<script>`/scoped `<style>`), so the policy is **hash-based with no `'unsafe-inline'`**.

`astro.config.mjs` declares only the non-hashed directives; `script-src 'self' 'sha256-…'` and the `style-src` hashes are appended automatically:

```js
security: {
  csp: {
    directives: [
      "default-src 'self'",
      "img-src 'self' data:",
      "font-src 'self' https://fonts.gstatic.com",
      "connect-src 'self'",
    ],
    // resources REPLACE the default style-src sources, so 'self' must be re-listed.
    styleDirective: { resources: ["'self'", 'https://fonts.googleapis.com'] },
  },
},
```

**When adding a new external resource, update the allowlist or it is blocked at runtime:**

- External script/CDN → `scriptDirective.resources` (re-list `'self'`)
- External stylesheet / font CSS provider → `styleDirective.resources`
- New font host → the `font-src` entry in `directives`
- Image/CDN host, or a new fetch/API origin → `img-src` / `connect-src` in `directives`

**Two hard constraints:**

- CSP is **inert in `pnpm dev`** (Vite limitation). It only applies in `pnpm build` / `pnpm preview` — always verify a CSP-affecting change there, and check the browser console for `Refused to…` violations.
- Hashes **cannot** cover inline `style="…"` **attributes** (only `<style>`/`<script>` *elements*). Once Astro emits style hashes, browsers ignore `'unsafe-inline'`. Keep dynamic styles in utility classes or set them via the CSSOM (`element.style.x = …`, allowed from a hashed script) — never as markup style attributes.

## Dependencies

- Run `pnpm audit` before adding any new package
- This is a static portfolio — keep the dependency list minimal
- Any new npm package should be evaluated for: bundle size impact, maintenance status, and whether a vanilla solution exists

## No Sensitive Data in Static Output

- `pnpm build` should never output secrets, API keys, or internal URLs into `dist/`
- Cloudflare Workers environment variables are server-only by default — do not prefix with `PUBLIC_` unless the value is intentionally public
